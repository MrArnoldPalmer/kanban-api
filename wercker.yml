box: clojure
services:
  - id: mongo
build:
  steps:
    - script:
      name: test
      code: |
        lein test
    - script:
      name: build
      code: |
        lein uberjar
dev:
  steps:
    - script:
      name: env
      code: env
    - internal/watch:
      code: lein run
      reload: true
