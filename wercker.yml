box: clojure
services:
  - id: postgres
    env:
      POSTGRES_PASSWORD: kanban
      POSTGRES_USER: kanban
build:
  steps:
    - script:
      name: test
      code: |
        lein test
    - script:
      name: build
      code: |
        lein uberjar
dev:
  steps:
    - script:
      name: env
      code: env
    - internal/watch:
      code: lein ring server-headless 4000
      reload: true
